import { Expletus_Sans, JetBrains_Mono } from "next/font/google";

export const expletus_sans = Expletus_Sans({
  weight: "500",
  subsets: ["latin"]
});

export const jetbrains_mono = JetBrains_Mono({
  weight: "400",
  subsets: ["latin"]
});
